## Init Project
This project should be used in Linux or Window system, bitsandbytes library don't support Mac.
1. run `poetry install` to install the dependency of project
2. run `poetry run python main.py` to run DPO process of the project
3. run `poetry run python eval_main.py` to evaluate trained model on Antropoic dataset.